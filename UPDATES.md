# UPDATES

## 2026-04-01 Phase 1 実装

### やったこと
- Express サーバー構築（`server/index.js`）
- 音声ファイルアップロード・配信・削除 API（`server/routes/audio.js`、multer使用）
- ボタン設定 CRUD API（`server/routes/buttons.js`、JSON永続化）
- iPad操作用メイン画面（`frontend/index.html` + `app.js`）
  - 4×4グリッド、2btnモード（タップ再生/停止）、1btnモード（長押し）実装済み
- 設定画面（`frontend/settings.html` + `settings.js`）
  - 曲アサイン・ラベル・再生モード・フェードイン/アウト設定UI
- Web Audio API ラッパー（`frontend/js/audio.js`）
  - フェードイン/アウト対応、iPad Safari AudioContext unlock対応
- FastClick 導入済み（CDN経由）
- `install.bat`（npm install）、`start.bat`（サーバー起動）
- `.gitignore`（node_modules / 音声ファイル / buttons.json を除外）

## 2026-04-02 1btnバグ修正 & AllKill実装

### やったこと
- 1btnモードの多重再生バグを修正（`Symbol`による非同期ロードの競合防止）
- All Killボタン（緊急停止）を実装
  - 既存のフェード設定を活かしつつ、最大1秒に制限して停止
  - ヘッダーを `fixed` 化し、スクロール中も常にアクセス可能に
  - 停止時にフロントエンドの全ボタンUI状態をリセット
- `README.md` の進捗状況更新

### 次にやること
- ボタン数・グリッドサイズのカスタマイズ機能
- 同時再生の排他・多重制御設定
- 設定のエクスポート・インポート

## 2026-04-02 PWA対応 & UI改善

### やったこと
- PWA（ホーム画面への追加）対応
  - モダンなパステルカラーのアプリアイコン生成・配置
  - `manifest.json` の作成と各HTMLへのメタタグ追加（iOSスタンドアロンモード対応）
- UI・アクセシビリティの改善
  - 固定フッターの追加（コピーライト「©どどど素人 dododo-shirouto 2026」とX(Twitter)へのリンク）
  - 設定画面の「戻る」ボタンを削除（iFrame無限ループ防止、親モーダルの「✕」ボタンへ集約）
  - スタイル調整（ヘッダー・フッターの固定、背景のぼかし効果、要素の重なり防止）
- `README.md` の進捗状況更新

## 2026-04-02 フェードアウト中の再プレイ対応

### やったこと
- フェードアウト中の再プレイ挙動の改善
  - `audio.js` に `stopping` フラグ（遺言フラグ）を導入
  - フェードアウトが開始されたパッドを「非再生中」として扱うよう `isPlaying` を更新
- フェードアウト中に再度パッドが押された際、古い音源を即座に破棄（Kill）して新しい再生を開始するように修正
  - フェードアウト開始時にプログレスバーなどのUIを即座に消去するよう `getProgress` を調整
- `README.md` の進捗状況更新

## 2026-04-03 初期ローディングUIとスマホインポート対応

### やったこと
- 初回の音楽読み込み状態の視覚化
  - メイン画面に「タップして開始」・「読み込み中...」のオーバーレイを追加
  - オーバーレイで初期のアクティベーションを促しつつ、背後のポン出しグリッドをグレーアウト
  - ローディング中も設定画面の展開・操作を可能に維持
- モバイル向けインポート対応の改善
  - 設定画面のファイルアップロード操作エリアを `div` から `<label>` に変更し、スマホでのタップによるOSネイティブのファイル選択ダイアログ呼び出しを確実化
  - 親画面がローディング中の間は、設定内の曲選択 `<select>` を無効化し競合を防止
- サーバ起動時のポート指定対応
  - `start.bat` に `--port` 引数処理を追加（例: `start.bat --port 8080`）し、環境に合わせてポート番号を変更可能に
- `README.md` の進捗状況更新

## 2026-04-09 終了タイマー機能追加

### やったこと
- 各BGMボタンに指定秒数で自動停止（またはフェードアウト開始）する「終了タイマー」機能を追加
- 設定画面に終了タイマー秒数の入力UIおよび、有効/無効の状態を示すアイコンを追加
- PAD画面の残り時間表示の下に、目立つ色（黄色）でタイマー作動までの残り時間表示を追加
- Web Audio APIラッパー (`audio.js`) に `setTimeout` による指定秒数後のフェードアウト/停止ロジックを実装
- タイマー作動時刻から逆算した残り時間を取得可能に拡張
